services:
  database:
    image: postgres:17.5-alpine
    container_name: elektronika_db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    command: >
      postgres
      -c shared_preload_libraries=auto_explain,pg_stat_statements
      -c auto_explain.log_min_duration=10
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  # server:
  #   build:
  #     context: server
  #     dockerfile: Dockerfile
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./server:/app
  #     - /app/node_modules
  #   depends_on:
  #     database:
  #       condition: service_healthy
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.server-router.entrypoints=web"
  #     - "traefik.http.routers.server-router.rule=PathPrefix(`/api`)"
  #     - "traefik.http.services.server-service.loadbalancer.server.port=5150"
  #
  # client:
  #   build:
  #     context: client
  #     dockerfile: Dockerfile
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./client:/app
  #     - /app/node_modules
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.client-router.entrypoints=web"
  #     - "traefik.http.routers.client-router.rule=PathPrefix(`/`)"
  #     - "traefik.http.services.client-service.loadbalancer.server.port=5173"
  #
  # traefik:
  #   image: traefik:v3.6.5
  #   command:
  #     - "--api.insecure=true"
  #     - "--providers.docker=true"
  #     - "--entrypoints.web.address=:8000"
  #   ports:
  #     - "8080:8080"
  #     - "8000:8000"
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock:ro
